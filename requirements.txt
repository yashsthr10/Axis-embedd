# Production dependencies — pinned for reproducible builds.
# Dev tools: pip install -e ".[dev]"
# Eval tools: pip install -e ".[eval]"

torch>=2.1.0
safetensors>=0.4.0
sentencepiece>=0.2.0
transformers>=4.36.0
pyyaml>=6.0
numpy>=1.24.0
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
httpx>=0.26.0
